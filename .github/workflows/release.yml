name: Release

on:
  push:
    tags:
      # An Obsidian plugin tag is the bare manifest version with NO `v` prefix.
      # The v-prefixed shape is matched too, and rejected by the guard below
      # with an explanation: a tag GitHub never matches would run nothing at
      # all, and "nothing happened" is the hardest release failure to read.
      - '[0-9]+.[0-9]+.[0-9]+'
      - 'v[0-9]+.[0-9]+.[0-9]+'

jobs:
  release:
    name: Build and attach the plugin assets
    runs-on: ubuntu-latest
    permissions:
      contents: write # uploads assets onto the release for this tag
    steps:
      # GATE: the notes are not the workflow's to write. A CHANGELOG entry is
      # deliberately one or two lines for a contributor, which makes poor
      # release notes for a reader deciding whether to upgrade. A human writes
      # the draft first and this refuses to publish without one.
      #
      # Found by listing rather than `gh release view <tag>`: a draft's tag is
      # not a git ref, so addressing one by tag can match nothing, change
      # nothing and still exit 0.
      - name: A release draft for this tag exists and has notes
        if: github.event_name != 'workflow_dispatch'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          release="$(gh api "repos/$GITHUB_REPOSITORY/releases" --paginate \
            --jq "[.[] | select(.tag_name == \"$GITHUB_REF_NAME\")] | first")"
          if [ -z "$release" ]; then
            echo "::error::no release for $GITHUB_REF_NAME. Write the notes first:" \
                 "gh release create $GITHUB_REF_NAME --draft --notes-file NOTES.md"
            exit 1
          fi
          body="$(printf '%s' "$release" | jq -r '.body // ""' | tr -d '[:space:]')"
          [ -n "$body" ] \
            || { echo "::error::the release for $GITHUB_REF_NAME has no notes"; exit 1; }
          printf '%s' "$release" | jq -r '"draft=\(.draft) tag=\(.tag_name)"'

      - uses: actions/checkout@v4

      # Cheapest check first: a mismatched tag is rejected before the install.
      - name: Verify the tag agrees with the manifest
        run: scripts/check-release-tag.sh "$GITHUB_REF_NAME"

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      # package-lock.json resolves the carve engine as git+ssh, and npm only
      # reaches it on a runner because it falls back to HTTPS on its own. CI
      # installs fine without this, but a release must not rest on that
      # fallback. Both lines write the SAME config key, so both need --add:
      # a plain set would replace the first rewrite with the second and leave
      # the ssh:// form uncovered.
      - name: Prefer HTTPS for Git dependencies
        run: |
          git config --global --add url."https://github.com/".insteadOf ssh://git@github.com/
          git config --global --add url."https://github.com/".insteadOf git@github.com:
          git config --global --get-all url."https://github.com/".insteadOf

      - run: npm ci

      # The same gate CI runs: tests, typecheck, and the production build that
      # writes main.js. A release is not the place to find out the bundle does
      # not compile.
      - run: npm run test:all

      # main.js is a build artifact and is not in the repo tree, so this is
      # what stands between a green workflow and a release carrying two of the
      # three files a user needs.
      - name: Verify the three release assets exist
        run: scripts/check-release-assets.sh

      # The release for this tag usually already exists as a draft holding the
      # notes, so `gh release create` would fail on it. Upload onto whatever is
      # there and create one only when there is nothing. This step never flips
      # a draft to published; the last step in the job does that, once the
      # assets are verified.
      - name: Attach main.js, manifest.json, and styles.css
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ github.ref_name }}
        run: |
          if gh release view "$TAG" >/dev/null 2>&1; then
            echo "Release $TAG exists; uploading assets onto it."
          else
            echo "No release for $TAG yet; creating a draft to hold the assets."
            gh release create "$TAG" --draft --title "$TAG" --notes "Release $TAG"
          fi
          gh release upload "$TAG" main.js manifest.json styles.css --clobber

      # Ask GitHub what the release carries rather than trusting the upload's
      # exit code. A partial upload leaves a release that installs nothing.
      - name: The release actually carries all three assets
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ github.ref_name }}
        run: |
          names="$(gh release view "$TAG" --json assets --jq '.assets[].name' | sort | tr '\n' ' ')"
          echo "Assets on $TAG: $names"
          missing=0
          for asset in main.js manifest.json styles.css; do
            case " $names " in
              *" $asset "*) ;;
              *) echo "::error::$asset is not attached to release $TAG."; missing=1 ;;
            esac
          done
          exit $missing

      # A registry publish is not finished until the release page exists. The
      # tag is pushed, CI publishes, the registry answers 200, and "released"
      # feels done - while `/releases` still shows a draft, so a reader has no
      # notes and no changelog for the version they just installed. Doing it
      # here rather than by hand also means the draft stops being a thing
      # someone has to remember to publish at the right moment.
      #
      # Last, deliberately: a draft that stays a draft is recoverable, and one
      # published beside a failed publish is not.
      - name: Publish the release notes
        if: github.event_name != 'workflow_dispatch'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          id="$(gh api "repos/$GITHUB_REPOSITORY/releases" --paginate \
            --jq "[.[] | select(.tag_name == \"$GITHUB_REF_NAME\" and .draft)] | first | .id // empty")"
          if [ -z "$id" ]; then
            echo "No draft release for $GITHUB_REF_NAME; nothing to publish."
            exit 0
          fi
          # `tag_name` goes with every PATCH of a release. Omit it on a DRAFT
          # and GitHub replaces the tag with a generated `untagged-*`
          # placeholder, because a draft's tag is a string on the object rather
          # than a git ref - so the field has nothing to fall back to.
          # `-F` (not `-f`) for draft, so it is sent as a boolean.
          gh api "repos/$GITHUB_REPOSITORY/releases/$id" -X PATCH \
            -f tag_name="$GITHUB_REF_NAME" -F draft=false --jq '"published \(.tag_name)"'
          # Read it back: exit status does not tell you whether the tag survived.
          gh api "repos/$GITHUB_REPOSITORY/releases/$id" \
            --jq '"draft=\(.draft) tag=\(.tag_name)"'
